Go over all the types of sets / subsets. Especially not subset / superset

>[!danger] Remember!
>You must justify all steps in proofs / assignments in MATA31

- Let $a, b \in \mathbb{R}$. Then $ab = 0 \iff a = 0\ or\ b = 0$

for the d = c^2 question, when a = b (which is wrong), why is the rest of the solution correct?

d = c^2 is an implication, so we have to write a step to use it. 
Assume d = c\^2
Then...

### Fancy absolute things you can do
![[Pasted image 20240910174353.png|300]]
- The solution set is (-4, 2/3)
- If both sides are absolutes, you can square both sides. Since ^2 is the same as absoluting, we can remove the absolute value symbols and solve 
