Declare a constant by writing a variable name in all caps. For instance, `MAXIUMUM_SIZE = 500` would be a constant

Any variable with all caps like that shouldn't be changed throughout the program (python will still let you though)