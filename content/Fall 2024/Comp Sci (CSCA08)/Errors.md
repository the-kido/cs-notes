NameError --> Called when a variable is referenced but not declared
```python
apple = banana # banana is not declared, so this will throw an error
```


AttributeError --> Called when a method does not exist in an object. i.e you cannot call a.find('!') if a is an integer instead of a string!
