`=` is the assignment operator. Reads as "gets" in statements
`==` reads as "equals". Don't get it mixed up

A statement is an action / command
- Assignment statements
- Print statements
Expressions are a combination of operations / values
- 5 * 3 is an expression.
- In k = 2 + 3, then `2 + 3` part is the expression

