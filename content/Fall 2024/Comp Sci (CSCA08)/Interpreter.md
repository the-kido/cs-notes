 A program that translates code into machine executable code (byte code), but *not* machine code.  

 The interpreter is used via
- The Python Shell
- An IDE (which is more convenient to type in / can check for errors)
