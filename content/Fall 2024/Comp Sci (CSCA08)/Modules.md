A library of functions, usually.
For example, there's a math module with ceil, sqrt, etc.

You can get access to them with `import`
```python
import math

square_root = math.sqrt(4.0)
```

You access stuff in a module with `.`
