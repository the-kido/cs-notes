It's some UTSG shenanigans 

Pre(n) is like the base case. Means the input "n" is valid, basically.
Post(t, n) is what holds when we show Pre(n) holds.
- We can use Post(t, n) to show, for instance, Pre(n+1) holds, if i understand correctly? And that would show induction

