### 1. 

a)
I skipped lots of the math but the idea is there! Note: base case is it's own branch *always*

a236 = (a_15)^2 + 2a_12 = 255
- a_15 = (a_3)^2 + 2a_3 = 15
	- a_3 = a_1 + 2a_1 = 3
		- a_1 = 1

b)

a_4 = a_2^2 + 2a_2 = 9 + 6 = 15 (wtf this always works?)
- a_2 = 1 + 2 = 3
	- a_1 = 1
Clearly it should for for all n >= 2. a_2, a_3, a_4, and im sure a_5 and etc. also hold.
We wanna show, $\forall n \geq 2, P(n)$ where $P(n) \equiv 3 | a_n$
and we do this with blimmin' induction

Let n be arbitrary. Assume n >= 2
Assume IH: $\forall k \in N, 2 \leq k < n \implies P(k)$ (P(k) holds for all $k$ less than $n$

Case: n = 2
- Then a_2 = 3 and 3 | 3 by... observation?
Case: n = 3
- Then a_3 = 3 and 3 | 3 by obvious
Case: $n \geq 4$
- Because n >= 4 > 1, so $\sqrt n \geq 2$. Also, $n > \sqrt n$ since, again, $n>1$ 
- Therefore by IH, we have $P(\sqrt n) \equiv 3 | a_{\lfloor \sqrt n \rfloor}$
- Then, $a_n = (a_\sqrt n)^2 + 2a_\sqrt{n}$
- We know there is an $i$ such that $a_{\sqrt n} = 3i$ 
- So $a_n = 9i^2 + 6i$
- $=3(3i^2+2i)$
- which is also divisible by $3$ ($3i^2 + 2i$ witnesses $\exists i \in \mathbb Z, x = 3i$ 

c)

P(16) 
- P(4)
	- P(2)
		- P(1)
For the specific examples i can just write out the predicate instead of what i did above

$3 | a_{16}$
- $\because 3 | a_4$
	- $\because 3|a_2$
		- $\because 3 | a _1$ 

### 2.
a)

$(x+1/x)(x^n + 1/x^n)$
$=x^{n+1} + \frac{1}{x^{n-1}} + x^{n-1} + \frac{1}{x^{n+1}}$
$= c_{n+1} + c_{n-1}$

But the thing I didn't notice is that, at the start, $(x+1/x)(x^n + 1/x^n) = c_1 \cdot c_n$
which eventually equals $c_{n+1} + c_{n-1}$
which is dumb. how the heck was i supposed to make that correlation 

("instantiating")
$c_1 \cdot c_0 = (x + 1/x)(2) = x + 1/x + 1/x + x = c_{1} + c_{-1}$
$c_1 \cdot c_1 = (x + 1/x)^2 = 1 + 1 + x^2 + 1/x^2$
eyeyeyey

b)
Since we know $c_1 \cdot c_n = c_{n-1}+ c_{n+1}$,
- we can isolate for $c_n$ to gettt:
	- $c_n = \frac{c_n}{1 + 1/x}$
- wait uhhh maybe not a good thing to do 
- Perhaps we can use the other formula
- $c_1 \cdot c_{n+1} = c_{n+2} + c_{n}$
- $\therefore c_n = c_1 \cdot c_{n+1} - c_{n+2}$
	- I derived it differently but is equivalent. 
	- However it makes my proof aids in comparison.
	- Using the formula that $c_n = c_1\cdot c_{n-1} - c_{n-2}$ is so much better
- $c_{n+2} = c_1\cdot c_{n+1} - c_n$

nice

c)
I got this ($P(n) \equiv x^n + 1/x^n \in \mathbb Z$)

Let $x \in \mathbb R^*$ be arbitrary. Assume $x + 1/x \in \mathbb Z$
Let $n \in \mathbb N$
Assume IH (for all k < n, P(k) holds)

Case: n = 0:
- 1 + 1 = 2 \in Z
- So P(0) holds
Case: n = 1:
- x + 1/x \in Z by given
- So P(1) holds
Case: n >= 2
- Since $n \geq 2$, $n - 1 \geq 0$, so $n - 1 \in \mathbb N$ and $P(n-1)\equiv x^{n-1} + 1/x^{n-1} \in Z$ holds
- Same for $P(n-2)\equiv x^{n-2} + 1/x^{n-2} = c_{n-2} \in Z$ (by IH)
- We know that $c_{n} = c_1 \cdot c_{n-1} - c_{n-2}$
- Since $c_1, c_n-1 and c_n-2$ are all integers, c_n is an integer

$x^3 + 1/x^3 \in Z$
- $x^2 + 1/x^2 \in Z$
	- $x^1 + 1/x^2 \in Z$ 
	- $x^0 + 1/x = 1 + 1 \in Z$
- $x^1 + 1/x^1 \in Z$

#todo if time. I didnt read the instructions. it would take forever, admittedly 



### 3. 💀

Im gonna ignore it for now, honestly


### 4. 
a)
$b_h = 2^h$ is true but only for a perfect tree. 
$b_0 = 1$ always
OKAY I get it now
For any tree (by tree, I mean the root) one subtree can have the height h-1 and the other can *also* have h-1. There are b_h-1 ways to have either, so by multiplication rule, the ways the tree has two subtrees with h-1 height must be `b_h-1 * b_h-1`
Otherwise, one half must have a height h-1, and the other has any of height 0 to h-2. You just add the ways you can have all those heights. 
$b_0 = 1$ 
$b_h = b_{h-1}^2 + 2 \cdot b_{h-1} \cdot \sum_{i=0}^{h-2} b_i$ for $h \geq 1$

I am NOT doing that proof 
### 5.

