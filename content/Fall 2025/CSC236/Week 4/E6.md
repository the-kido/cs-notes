
### 1. 
a) 
yeah...

b)
i)
Based on the proof, when $n=1$, it proves the instance when $m = 2$ 
So, when we prove $236$ of the consequent (that is, the 236 )
i still don't get it honestly 
#todo ?

ii)


### ignoring question 2

### 3
I think there's a massive typo.
Anyway yes, we can count recursively, either considering the base case of 1 element, or the parentheses situation (i.e. `(e1 + e_2)` = `(e_1) + (e_2)`
This gives the structural induction principle:
$(P(w) \wedge \forall w_1, w_2, P(w_1) \wedge P(w_2) \implies P(w_1 \oplus w_2) \wedge P(w_1 \otimes w_2)) \implies \forall e \in E, P(e)$

a)
```python


```
nah

c)
Your "algorithm" in math form is just the returned values.
1. W(w) = 1
2. if e1, e2 in E, then W((e1 + e2)) = W(e_1) + W(e_2) and same for \*

d)


### 6
