# Question 1

### 1
1. "Out of core processing"
	1. I dunno what this even is
	2. "Buffer management" / "caching" (e.g. if there's an item you frequently read/write to, you can cache it to make it faster)
	3. Indexing (instead of searching, just index to items -- uses more memory)
2. "Scaling up"
	1. Mutlithreading (same server more cores type deal)
3. "Scaling out"
	1. "Sharding" / replication
		1. Sharding (*partitioning*) is when you split data into many "shards" / pieces and put those pieces on different servers
		2. Replication means having many copies of data so it can run on several servers (reduces server load). Also has the benefit of safety / reliability (means if a server dies at least there's backup!)
### 2
- Logical independence
	- Changing relations shouldn't change the application (?)
	- DB Schema is decoupled from applications
- Physical independence
	- Doesn't matter if you're using an HDD, SSD, speed, etc. 
	- DB is decoupled from storage implement (hardware)


### 3
- Increased "plasticity" (scales for whatever you're doing)
- Higher availability
	- Provided by SLAs (?) 
- Reduced cost
	- You just pay as you go, instead of paying over what you need.


### 4
- The "SIN" is the "primary key" for customers cuz it's unique

The "address" entitty is its own thing. Customers might point to a unique address (like a class). Changing an address changes the address of all customers pointing to it. 


### 6

I took a photo (note though that the teams/fans thing is a many-many relationship)
 