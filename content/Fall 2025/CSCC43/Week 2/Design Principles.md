Avoid redundancy!
That is all.
- Like no attributes "studio-name" and "studio-addr". That is bad.
- Also don't have two attributes for the same purpose (like the studio's name shouldn't exist inside of "movies" if a relation lets you get the studio name another way..

(slide 15, 16 for lecture 2)