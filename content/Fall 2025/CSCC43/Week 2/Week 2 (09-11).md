

Relations (the "verbs") become the `name(keys,of,related,groups)` (refer to slide 30)
- For many-many, you would need to use *both* keys to serve as the *designated key* since both groups are required to identify the relationship
- For many-one, the key of the relation is just that of the "many". Its attributes will still contain keys of both groups, but the "second" one is interchangeable (Any one "name" satisfies the many-one relationship)

#todo go over the wife/husband example


Slide 31: redundancy is in the movie name (cuz duped) i think

32: When converting E/R to relational, dont include supporting relationships ?

For slide 33:
- At() has lname and hname as the key, since that's what the "logins" key is. 
- Then after the key, we include the key of the thing "at"-ed (as per the algo we did in slide 30). it is redundant though
- As such, if we notice this sorta thing happening, we can delete either Logins or At or Hosts
	- Since the host name is always contained in the Logins name, we can remove "hosts"

# Functional Dependencies


- If two tuples have the same attribute values (X), then something i missed it :(

Slide 42 has example. The reasonable FDs to assert is only based on the sample given tho
- If two Ks are depending on the same X, then X --> K1 & K2

#todo note the rules
title --> year reads as "title determines year" (which is doesn't so it's false. How to show false? #todo)

Superkey! Its flipping cold

X-->Y and Y-->X holds iff one-one relation
many-many is not possible (one side cannot determine the other side)

 