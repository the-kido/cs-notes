Review:
- for $\det$ cross row w/ most zeros
- Row operation keeps matrix the same (?)
	- (relevant for 2. Find determinant)
	- Oh it's for the reverse triangle thing
	- If reverse u just multiply the #'s on the diagonals
- $\det$ for $3\times3$ matrix
	- similar to the 2x2 case where left to right arrow is positive and right to left arrow is negative, u can just brute force the det 
	- It's easy cuz the arrow u multiply them all together and then add sign, then add all of what u get
	- Slide explains quite well with the $-(-4) + 0 + 0 + 0 + 0  + 0$ example

Eigenvalues
- [[Week 10 notes (Eigenvectors and Eigenvalues)]]
- if $A\vec v = \lambda \vec v$ where $\vec v \neq 0$ and $A$ is a matrix, then $\vec v$ is an eigenvector!
- (i think slides are missing the $\det$ confirm w/ previous notes #todo)
- $\det| A - \lambda I|$ is just the matrix of $A$ subtracted by the identity matrix $\times$ lambda!
- Then u factor and solve for lambda!
- Also recall Geo and Alg within [[Diagonalizability]] (it's easy!)
- #todo do question #2 (esp the "factoring" thing)

 Eigenvectors
 - U find the eigenvalues
 - Then you solve for $\vec x = (x, y,z)$
 - something abt one value being bound and the other two being free
	 - $s + t + u = 0$ eventually gets u $u = -s - t$ right. So $u$ is bound and $s, t$ are free. Those are the "values we pick" i still gotta review this  
# Course time
[[Fields]]
- Got a bunch of *axioms*
- $\oplus$ $\otimes$
- On exams she's asked to define a field/vector space #todo
- #todo what is a scalar again
- Irrational numbers does NOT include rational numbers
	- The diagram is a lie bruh
	- So it's not a field cuz not closed w/ multiplication: $\sqrt 2 \times \sqrt 2 = 2$ but $2$ isn't irrational. hhh
- $\mathbb R / \mathbb Q = \set{\text{Irrationals}}$ (all reals excluding rationals)
- Scalar = just any kind of number (so like a real number or irrational. it's just a category of fields)


#### Proof Question
This one is pretty easy!
for all a, b, c in Q, $a = a_1 /a_2, b = b_1/b_2, c = c_1/c_2$ where $a_i, b_i,c_i \in \mathbb Z$ and $a_2, b_2,c_2 \neq 0$ 
(we do a,b,c to knock two out at once)

1. Closure under $\oplus, \otimes$
	1. $a \oplus b = \frac{a_1b_2 + a_2b_1}{a_2b_2} \in\mathbb Q$ (so it's closed!) (we can do the operations w/ field operations of integers)
	2. Same idea for the rest yeah no biggie
2. For $a + b  = b + a$ I wanna #todo it cuz i wanna know *why* u can do the certain operations (i think we just assume the integers are fields, and then we can pull the axioms of fields for integers specifically, but the step "breaking it up" idk how to show that)
3. She did the flipping LS = RS thing (heresy!)
4. 