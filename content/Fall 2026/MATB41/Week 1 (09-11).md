HW has stuff u dont see in lecture/tutorial

$\frac{df}{dx} \in \mathbb R$ #todo Go over what this "means" (cuz it is a number, but wat kinda number again)

>[!defn] The <span class="definition">Graph</span> of $f: X \to Y$ is $\operatorname{graph}f$ (we just do this later lol)
>But graphs are sets. It's all points mapping input to output


>[!example] $f(x) = x^2$
>The graph is $graph(f) \subseteq \mathbb R^2$ 

But what about $f(x, y) = x^2 + y^2$? 

We can use a table and then "connect the dots"

#todo Draw what Parker drew. Maybe bring paper lowkey


Tool: CalcPlot3d

#### Limits
oh no
In 2d you just have left and right limits. 

In 3d there are uncountably many directions. #todo what's the semantics vs. infinite?

>[!example]
>Paths (a span of vectors) are represented like $c_1(t) = (t, t)$ ok not bad
>
>So finding the limit "along a path" what does that "mean" cuz at a point it gives u the slope but.... not here.
>It's 1/2 for (t, t) probably cuz it's actually constant?
>
>Okay so the paths are the infinite "directions".  
>(What would a "path" be in 2D?)


#### Derivatives
We wanna measure RoT of this function
- Direction affects RoT (changing x OR y changes RoT)
- $\frac{\partial f  } {\partial x}$ means "derivative of $f$ in the $x$ direction". And $x$ is a vector i guess?
- The partials together gives "the" derivative

