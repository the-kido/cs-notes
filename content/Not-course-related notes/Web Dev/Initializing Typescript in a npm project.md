`npm install --save-dev typescript`

``npx tsc --init`` 
- Creates a `tsconfig.json` file for your convenience

`npm install --save-dev webpack webpack-cli ts-loader`
- For when you want to bundle your code (you might need to change `webpack.config.js`)
