`Registers > Cache > RAM > Disk` in terms of speed; opposite way in terms of "max storage".
 - CPUs might have 30+ registers. They are *directly* what the CPU uses to compute things.
 - Outside of registers, the other levels are accessed indirectly. Cache, for instance, is mediated by registers before being utilized by the CPU.
