SERIES TIME (we don't talk about the mid-term)

Integral thingy

I.T:
>[!proof] Proof of IT
>Suppose 
>(1) f(x) > 0 on [1, \infty]
>(2) f cont on [1, \infty]
>(3) f(x) is decreasing on [1, \infty]
>(4) $a_n = f(n) \forall n \in \mathbb N$
>WTS $\sum_{n=1}^\infty a_n$ converges iff $\int_a^\infty f(x)\;dx$ converges.
>
>We first draw a picture. yay. (i took a photo)
>Notice that the points formed by $a_n$ not only lay on $y=f(x)$, but we can use these points to create a Riemann sum. We know the points form a Riemann sum, $\delta x = 1$!
>- We know $a_2(1) + a_3(1) + \dots + a_n(1)  \leq \int_1^n f(x)\;dx$ because f is decreasing and this is for right Riemann sum. `(*)`
>	- We multiply by 1 cuz that's what the width is.
>- The left Riemann sum will give an overestimate
>	- $\int_1^n f(x)\;dx  \leq a_1 + a_2 + \dots + a_{n-1}$ `(**)`
>
>We r gonna do the hard direction $\leftarrow$ for the biconditional
>
>Suppose $\int_1^\infty f(x)\;dx$ converges (5)
>WTS $\sigma_{n=1}^\infty a_n$ converges.
>- No GS test
>- Diff test doesn't help prove convergence
>- ...so we have to use definition!?
>
>WTS $\lim \limits_{n\to\infty} S_n$ exists (as a number;)
>WTS ${S_n}$ converges #todo whaa. Maybe i should write an algorithm for defn proofs
>We will use `(*) and (**)`
>
>Note `(*)` $\implies a_1 + a_2 + \dots+ a_n \leq a_1 + \int_a^n f(x)\;dx$ by adding $a_1$ (the left side is $= S_n$)
>- Note $a_1 \geq 0$ by (1) and (4); $\int > 0$ by (1) and (3) def int prop.
>
>$\implies a_1 + a_2 + \dots+ a_n \leq a_1 + \int_a^n f(x)\;dx \leq a_1 + \int_1^\infty f(x)\;dx$ (by (5) and (1)
>Note that $a_1 + \int_1^\infty f(x))\;dx$ evaluates to some $M \in \mathbb R^+$
>So $S_n \leq M, \forall n \in \mathbb N$
>I.e. $\set{S_n}$ is bounded above
>
>Observe $S_{n+1} = S_n + a_{n+1}$ where $a_{n+1} > 0$ by 1 and 4
>$\geq S_n$ 
>So $S_{n+1} \geq S_n, \forall n \in \mathbb N$
>
>Therefore $\set{S_n}$ is increasing
>$\therefore$by BMCT, $\set{S_n}$ converges,
>so $\lim \limits_{n\to\infty} S_n$ exists
>So $\sum a_n$ converges!


Wow lots to unpack there.









