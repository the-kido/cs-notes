darboux exciting

the heights of each rect are the sups and infs

for $m_i = \inf{f(x) | x \in [x_{i-1}, x_i]}$, that inside set is a bunch of points between the two points in the partition. Note that the inf is just the minimum as the interval is closed.

The distance between partitions is $x_i - x_{i-1}$ b/c final - initial

darboux isnt founded w/ the notion of area. whereas with riemann sum it was all figured out b/c we wanted area, instead darboux's integral stuff just so happens to also give the area under a curve.

[[Supremum and Infinium (and max and min)#Least Upper Bound Property (Completeness axiom)| Remember the completeness axiom]]



Consider $g(x)$ for 1 if $x \in \mathbb Q$ and -1 if $x \not \in \mathbb Q$

Compute U(g, P) for *any* partition P of [0, 3]

Ans: we gotta let $P = \set{x_i}_{i=1}^n$ be arbitrary partition of [0,3]
For each i = 1, 2, 3... n, we need $M_i$
$M_i = \sup\set{g(x) | x \in [x_{i-1}, x_i}$
- we needa know what the set is!
- g(x) can be -1 or 1 by density of rational and irrational in R
SO, $M_i = sup{-1, 1} = 1$. WOW isnt that wack.

Therefore U(g, P) = $\sum_{i=1}^n M_i(x_i - x_{i-1})$
$\sum_{i=1}^n 1\cdot(x_i - x_{i-1})$
$= (x_1 - x_0) + (x_2 - x_1) + ... + (x_{n-1} - x_{n-2}) + (x_n - x_{n-1})$ by $\Sigma$ not def
$-x_0 + x_n$ by epic pattern recognition. A [[Telescoping Series|telescopic sum]]
= 3 - 0 = 3

#todo we needa be able to compute darboux sums. yay yippee.



### Darboux def'n of definite integral.
We just use Darboux sums instead of Riemann sums.
>[!quote] Let $a, b \in \mathbb R, a < b$
>Suppose f is bounded on [a,b].
>We say f is integrable on [a,b] / $\int_a^b f(x)dx$ exists IFF $\sup \set{ L(f, P) | P \text{ is any partition of } [a,b] }$ =  $\inf \set{ U(f, P) | P \text{ is any partition of } [a,b] }$ = $\int_a^b f(x)dx$

- is easier to use to prove that an integral *does not exist*. 

$\mathbb I$ can denote irrational


Prove that f(x) 1 if $x \in Q$ and -1 if $x \in \mathbb I$ is NOT integrable on $[0,1]$ by Darboux def'n
WTS LHS != RHS

let $P = \set{x_i}_{i=0}^n$ be an arbitrary partition of [0,1]
for i = 1, 2, ... n
- $m_i = inf\set{ f(x) | x \in [x_{i-1}, x_i]}$
	- Note that f(x) = 0 or 1 since Q and I are dense in R
- = inf{1, 0} = 0
- $M_i = sup\set{f(x) | x \in [x_{i-1}, x_i]}$ = sup{0, 1} = 1 for same reason as above

So $L(f, P) = \sum_{i=1}^n m_i (x_{i-1} - i) = \sum_{i=1}^n 0 = 0$
Also, $U(f, P) = \sum_{i=1}^n M_i (x_{i-1} - x_i) = \sum_{i=1}^n (x_{i-1} - x_i) = 1-0=1$ by telescopic sum thingy.
>You can also evaluate $\sum_{i=1}^n (x_{i-1} - x_i)$ *geometrically* b/c adding all the lengths of a subinterval of an interval, it should just be the length of the entire interval.
 $\sum_{i=1}^n (x_{i-1} - x_i) = length([0, 1]) = 1$ as well.

anyway after that side-tangent we have
Therefore for all P partitions of [0, 1], U(f, P) = 1 and L(f, P) = 0 because P was arbitrary.
Therefore sup{ L(f, P) | P any partition of [0, 1]} = sup{0} = 0 
and
inf{ U(f, P) | P any partition of [0, 1]} = inf{1} = 1 
Since 1 != 0, f is not integrable on [0, 1]


we will NOT compute integrals with Darboux. We just use it to prove that functions are not integrable; that the definition fails.

