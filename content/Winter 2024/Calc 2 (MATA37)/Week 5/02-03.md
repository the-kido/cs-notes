>Future me: You can see how I did this in [[Fundamental Theorem of Calculus II (FTOC II)]]
>I did it twice... woops.


epic epic yeyeyey

The hard question again (lolol review)

$H(x) = \int_{sinx}^{cosx} tan^{-1}x dx$.
We wanna find H'(x)..
We wanna use FTOC II.
$f(t) = \tan^{-1}(t)$ is an inverse trig function and so cont on$ dom(f) = R$. In particular, $f$ is cont WLOG on $[sin(x), cos(x)] \subset R$.

Define $F(x) = \int_c^x f(t)\; dx$ for some constant $C$ (later we add: between $sin(x)$ and $cos(x)$)
Therefore $H'(x) = d/ dx\int_{sinx}^{cosx} f(t)dt$ 
Note that $x$ is constant w.r.t. $t$. in the integral.
$=d/dx(\int_{sinx}^c f(t)dt+ \int_c^{cosx}f(t)dt)$ by union property for some constant c between $sin(x)$ and $cos(x)$.
then we swap yeye

$$H'(x) = d/dx(-\int_c^{sinx}f(t)dt + \int_c^{cosx}f(t)dt)$$
Remember that 'x' is a dummy variable. We can replace $x$ in $F(x) = \int_c^x f(t)dt$ with $sin(x)$ (and vice versa)
So we got big $F$ now, as we wanted.
$$=d/dx(-F(sin(x)) + F(cos(x))) \text{ by def'n in hypothesis}$$ Then we just finish taking the derivative.
$$=F'(sinx)cosx + F'(cosx)\cdot(-sin(x)) \text{by diff rules}$$
$$=-cos(x)f(sin(x))-sin(x)\cdot f(cos(x))\text{ by FTOC II b/c }F'(\cdot) = f(\cdot)$$
$$-cos(x)arctan(sin(x)) - sin(x) \cdot arctan(cos(x)) \text{ by f def'n}$$
And that is all!

End of Chapter 4.7 yeyeye. 

