### p4


#### Fields

A1 & A2. ⊞ and ⊡ are associative and communicative.
A3: There exists an <span class="emphasis">additive identity</span> $\hat 0 \in F$ and <span class="emphasis">multiplicative identity</span> $1 \in F$ such that 

A4: For each $x \in F$, there is an additive inverse $x' \in F$ such that $x \boxplus x' = \hat 0$
- We needed to define A3 first since otherwise the zero element thingy doesn't exist
A5 For each $x \in F$ except $\hat 0$ there is a multiplicative inverse $x^-1$ such that $x \boxdot x^{-1} = 1$
A6: The multiplication operator distributes over the addition operator (like foiling)

Pass all 6 of these will make you a field! 

The order in this definition is important. 
- You cannot easily prove the axioms after A2 without first showing A2 is true.

Examples of fields
- $\mathbb{R}$
- $\mathbb{Q}$
(You don't need to prove they're fields. We can assume they are)
Examples of NOT fields
- $\mathbb{N}$ (It has no additive inverses)
	- #todo Can't we say that A3 fails since there is no "0"?
- $\mathbb Z$ (it has no multiplicative inverses)

# P5 complex numbers
>[!note] The imaginary number is $i$ such that $i^2 = -1$ or $i =\sqrt{-1}$

>[!note] Complex numbers are numbers in the form: $z = a + bi$ where $a, b \in \mathbb R$.
>In this case
>1. $Re(z) = a$; a is the "real" part of the complex number
>2. $Im(z) = b$; b is the "imaginary" part of the complex number.


As we have mentioned, complex numbers form a field. 
That means it has an additive identity (0) and multiplicative identity (1).
But with what operators? The operators that we will use are the standard addition and multiplication operators. 
Let $z_1 = a + bi$, and $z_2 = c + di$.
Let's show that two elements in the field add to another element of the field (to show imaginary numbers are a field)
Addition: $z_1 + z_2 = (a + bi) + (c + di)$
$= a + (bi + c) +  di$
$= a + (c + bi) + di = (a+c) + (b+d)i$



What about other properties? What about subtraction, division, and absolute values.
He said something abt defining -1 and that's throwing me off a bit. I will choose to ignore it. 

> [!note] Defining subtraction of complex numbers
> Simply extend the definition of addition ($z_1 + z_2 \to z_1 - z_2 =  z_1 + (-1)z_2$ )
> The rest is simple algebra (in slides if you wanna see)

For division, it is not so simple. We have to somehow use our multiplication, addition, and subtraction operators to create an operation that acts like division for the real numbers.


# p7
The <span class="definition">complex conjugate</span>:  $\bar z = a - bi$ OR $z^* = a - bi$

Then there's modulus. Derived via the graph.
Modulus is similar to absolute in that it gives the distance 

Then note that$\frac{z_1}{z_2} = \frac{z_1 \bar z_2}{|z_2|^2} = \frac{(a+bi)(c-di)}{(\sqrt{c^2 + d^2})^2}$
## P8
Vectors!1 All proofs to show vectors are fields are simple LS = RS

A <span class="definition">vector space</span> is a set V together with two binary operations called vector addition and scalar multiplication such that:
A1/A2: $\boxplus$ is associative and communitive
A3. There is an element $\textbf{0} \in V$ such that:
$\textbf{0} \boxplus x = x$ for all $x \in V$. We call 0 the zero vector of V 
A4: For each $x \in V$ there is an additive inverse $x' \in V$ such that 
$x \boxplus x' = \textbf0$
A5: 
- We don't rlly need the brackets cuz bedmas is a thing
A6: The field addition in F distributes over scalar multiplication in V.
$(c+d)\boxdot x = c \boxdot x \boxplus d \boxdot x$ 
(note that this is also what lets us factor)

> You can write vectors (like the zero vector) with an arrow like $\vec 0$
#todo in page 7 of the textbook.

A7: $\boxdot$ associates with scalar multiplication on $\mathbb{F}$
$cd \boxdot x = c \boxdot (d \boxdot x)$

A8: the "1' is the multiplicative identity from $\mathbb{F}$


> To define a vector space, you must have the set V, the definition of addition, and the definition of scalar multiplication
> e.g. to define The Vector Space $\mathbb R^n$ we would show:
> 1. The vector space of real <span class="emphasis">n-tuples</span> is given as: $V = R^n = \set{(x_1, x_2, \dots, x_n) : x_i \in R}$
> 2. Addition on $R^n$ ($\boxplus$) is
> $$(x_1, x_2, \dots, x_n) + (y_1, y_2, \dots, y_n) = (x_1+y_1, x_2 + y_2, \dots, x_n + y_n)$$
> 3. Scalar multiplication on $R^n$ ($\boxdot$) is
>$$c(x_1, x_2, \dots, x_n) = (cx_1, cx_2, \dots, cx_n)$$   4. Then you show all the 8 axioms hold. Most are true because "the real numbers form a field"

> By our definition above, we use the + to represent $\boxplus$ so whenever we use $\boxplus$  in the context of vector spaces, we can use the + that we assigned in our definition. 

### p9

## p10
A direct proof is proving a statement t/f using def'ns, axioms, lemmas, or theorems without assuming anything else. 


# Cards
![[Pasted image 20250113103403.png]]


- This is for the $\hat 0z = 0$ thing for z in V
![[Pasted image 20250113105226.png]]

