For the last question of the quiz (also in the slides)
We use the 3 solutions given by the set; we have variables A B C D to solve.
What's a parameter again aa
The answer has a set of only 1 answer (where x_2 - x_4 = 0)

A function where the input is mapped to a unique output

"Spanning sets are big. independent sets are small"
$S_1 \subset V \subseteq S_2$
$S_1 \subset S_2$
?